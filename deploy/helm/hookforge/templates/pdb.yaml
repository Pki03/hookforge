apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "hookforge.fullname" . }}
  labels:
    {{- include "hookforge.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "hookforge.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
